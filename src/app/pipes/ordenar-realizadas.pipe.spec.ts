import { OrdenarRealizadasPipe } from './ordenar-realizadas.pipe';

describe('OrdenarRealizadasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarRealizadasPipe();
    expect(pipe).toBeTruthy();
  });
});
